sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t stephengrider/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stephengrider/react-test npm test

after_success:
  - docker build -t lokic/multi-client ./client
  - docker build -t lokic/multi-nginx ./nginx
  - docker build -t lokic/multi-server ./server
  - docker build -t lokic/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push lokic/multi-client
  - docker push lokic/multi-nginx
  - docker push lokic/multi-server
  - docker push lokic/multi-worker
